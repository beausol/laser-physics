%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% Chapter file included in main project file using \input{}
%
% Assumes that LaTeX2e macros and packages defined in rgb_laser_physics.sty
%   are available
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

 \chapter{One-dimensional Single-Mode Laser Dynamics\label{chp:laser_dynamics_1d}}

In this chapter, we describe the dynamics of one-dimensional laser amplifiers and oscillators by applying the quasi-normal mode expansions derived in \sct{laser_resonators_1d_qnm} to the wave equation given by \eqn{wave_eqn_1d} and the density matrix evolution equations defined by \eqn{fls_mbe_rwa_pol} and \eqn{fls_mbe_rwa_pop_diff}. Under the right experimental conditions, these single-mode representations (approximate as they are) can provide remarkably illuminating descriptions of laser behavior, including optimum output coupling, frequency pulling, wave mixing, and mode-locking.

\input{files/laser_dynamics_1d_amp}
\input{files/laser_dynamics_1d_approx}
\input{files/laser_dynamics_1d_sml}

% \begin{equation} \label{eqn:laser_field_contrib}
%\bmc{E}\rt \equiv \bmc{E}^\mathcal{L}\rt + \bmc{E}^\mathcal{I}\rt + \bmc{E}^\mathcal{S}\rt,
% \end{equation}
%
%
%
% \section{Algorithm}
%
% \begin{enumerate}
%
% \item \emph{Initialize.}
%
% The initial conditions are $A_q(t = 0) = 0$ for all $q$, and $g(z, t = 0) = 0$. The intracavity injected field can be written as
% \begin{equation}
% E^\text{inj}(z_j, t) = -i t \frac{\exp\left(-\alpha_b z_j\right) \exp\left(i \Delta \omega z_j/c\right)}
% {1 - r \exp\left(-\alpha_b p\right)\exp\left(i \Delta \omega p/c\right)} E^\text{inc} \exp\left(-i \Delta \omega t\right)
% \end{equation}
%
% \item \emph{Integrate} $g(z, t)$.
%   \begin{enumerate}
%   \item \emph{Compute} $I\left(z_j, t_k\right)$
%   \begin{equation}
%   I\left(z_j, t_k\right) = \left| E^\text{inj}(z_j, t_k) + r \exp\left( \frac{z_j}{p} \ln \frac{1}{r} \right) \sum_q \exp\left( i\, 2 \pi q z/p \right) A_q\left(t_k\right) \right|^2
%   \end{equation}
%   \item \emph{Compute} $R_p\left(t_k\right)$
%   \begin{equation}
%   R_p\left(t_k\right) = \frac{\tau_c}{\tau_p} \sqrt{\frac{4 \ln 2}{\pi}} \exp\left[ -4 \ln 2 \left(\frac{t_k - t_p}{\tau_p}\right)^2 \right] g_0
%   \end{equation}
%   \item \emph{Update} $g\left(z_j, t_{k + 1}\right)$
%   \begin{equation}
%   g\left(z_j, t_{k + 1}\right) \longleftarrow -\tau_c \frac{I\left(z_j, t_k\right)}{J_\text{sat}} + R_p\left(t_k\right)
%   \end{equation}
%   or, for a standing-wave cavity,
%   \begin{equation}
%   g\left(z_j, t_{k + 1}\right) \longleftarrow -\tau_c \frac{I\left(z_j, t_k\right) + I\left(p - z_j, t_k\right)}{J_\text{sat}} + R_p\left(t_k\right)
%   \end{equation}
%   \end{enumerate}
%
% \item \emph{Integrate} $A_q(t)$.
%   \begin{enumerate}
%   \item \emph{Compute} $G_{q, q^\prime}\left(t_k\right)$
%
%   \begin{equation}
%   \begin{split}
%   G_{q, q^\prime}\left(t\right) &= \int_0^p \frac{d z}{p} \exp\left[-i\, 2 \left(q - q^\prime\right) \pi \frac{z}{p}\right] g(z, t)\\
%                                 &= \int_0^L \frac{d z}{L} \cos\left[-i \left(q - q^\prime\right) \pi \frac{z}{L}\right] g(z, t)
%   \end{split}
%   \end{equation}
%   where the second expression applies to a standing-wave laser resonator with $p = 2L$.
%
%   \begin{equation}
%   \begin{split}
%   G_{q, q^\prime}\left(t_k\right) &= \sum_j \exp\left[-i\, 2 \left(q - q^\prime\right) \pi \frac{z_j}{p}\right] g(z_j, t_k) \Delta z\\
%                                 &= \sum_j \cos\left[-i \left(q - q^\prime\right) \pi \frac{z_j}{L}\right] g(z_j, t_k) \Delta z
%   \end{split}
%   \end{equation}
%   where the second expression applies to a standing-wave laser resonator with $p = 2L$.
%
%   \item \emph{Compute} $P^\text{inj}_q\left(t_k\right)$
%
%   \begin{equation}
%   P^\text{inj}_q(t) = \int_0^p \frac{d z}{p} \exp\left(-i\, 2 q \pi \frac{z}{p}\right) \frac{1}{r} \exp\left( -\frac{z}{p} \ln \frac{1}{r} \right)
%   g(z, t) E^\text{inj}(z, t)
%   \end{equation}
%
%   \begin{equation}
%   P^\text{inj}_q\left(t_k\right) = \sum_j \exp\left(-i\, 2 q \pi \frac{z_j}{p}\right) \frac{1}{r} \exp\left( -\frac{z_j}{p} \ln \frac{1}{r} \right)
%   g(z_j, t_k) \Delta z \, E^\text{inj}(z_j, t_k)
%   \end{equation}
%
%   \item \emph{Update} $A_q\left(t_{k + 1}\right)$.
%   \begin{equation}
%   A_q\left(t_{k + 1}\right) \longleftarrow -\left( \ln \frac{1}{r} + \alpha_b p + i\, 2 \pi q \right) A_q(t_k)
%   + \half \sum_{q^\prime} G_{q, q^\prime}\left(t_k\right) A_{q^\prime}(t_k)
%   + \half P^\text{inj}_q\left(t_k\right)
%   \end{equation}
%
%   \end{enumerate}
%
% \end{enumerate}
